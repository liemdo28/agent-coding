{
  "task_id": "task-017",
  "title": "Minimum Edit Distance",
  "language": "javascript",
  "difficulty": "hard",
  "category": "algorithm",
  "description": "Compute the minimum edit distance (Levenshtein distance) between two strings. The allowed operations are: insert a character, delete a character, or replace a character — each with cost 1. Return the minimum number of operations to transform string `a` into string `b`.\n\nFunction signature: `function minEditDistance(a, b)`",
  "function_signature": "function minEditDistance(a, b)",
  "test_cases": [
    { "input": ["kitten", "sitting"],  "expected": 3 },
    { "input": ["",       "abc"],      "expected": 3 },
    { "input": ["abc",    ""],         "expected": 3 },
    { "input": ["abc",    "abc"],      "expected": 0 },
    { "input": ["sunday", "saturday"], "expected": 3 },
    { "input": ["a",      "b"],        "expected": 1 }
  ],
  "reference_solution": "function minEditDistance(a, b) {\n  const m = a.length, n = b.length;\n  const dp = Array.from({ length: m + 1 }, (_, i) =>\n    Array.from({ length: n + 1 }, (_, j) => (i === 0 ? j : j === 0 ? i : 0))\n  );\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      if (a[i - 1] === b[j - 1]) {\n        dp[i][j] = dp[i - 1][j - 1];\n      } else {\n        dp[i][j] = 1 + Math.min(dp[i - 1][j - 1], dp[i - 1][j], dp[i][j - 1]);\n      }\n    }\n  }\n  return dp[m][n];\n}",
  "tags": ["algorithm", "dynamic-programming", "string", "levenshtein"]
}
