# Local Agent UI backend
PORT=4001
LOCAL_AGENT_PROJECT=.

# Local LLM settings are configured in local-agent/config/default.json.
# The default endpoint is http://localhost:11434 and must remain loopback-only.
