# Sample React App

A demo project for testing the local-agent scanner.

## Setup

```bash
npm install
npm run dev
```

## Commands

- `npm run build` — production build
- `npm run test` — run tests
- `npm run lint` — lint source
